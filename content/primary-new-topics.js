const ex = (structureId, q, steps, answer) => ({ structureId, q, steps, answer });

function percentageLesson() {
  const percents = [10, 25, 40, 15], totals = [80, 120, 250, 300];
  return {
    title: "Percentages: parts out of one hundred", minutes: 22, order: 16,
    prereq: ["fractionEquivalence", "decimalPlaceValue", "fractionOfQuantity"],
    intro: "A percentage is a fraction whose whole has been imagined as 100 equal parts. Connecting percentages to fractions and decimals lets us calculate, compare and reverse them without learning unrelated tricks.",
    sections: [
      { h: "1. Fractions, decimals and percentages name the same value", body: ["The symbol % is read as 'per cent', which means 'out of one hundred'. Therefore 10% means 10 out of every 100 equal parts, or 10/100.", "A decimal uses place value to name the same share. Dividing by 100 moves the value into hundredths, so 10/100 = 0.10 = 0.1.", "A fraction may then be simplified without changing its value. We divide numerator and denominator by the same factor because that regroups the pieces without changing the shaded amount."], examples: percents.map((p) => { const g = p === 25 ? 25 : p === 40 ? 20 : p === 15 ? 5 : 10; return ex("convert_percent_forms", `Write ${p}% as a decimal and a fraction in simplest form.`, [`${p}% means ${p}/100 because per cent means out of one hundred.`, `Divide by 100 to write the same value as a decimal: ${p} ÷ 100 = ${p / 100}.`, `Simplify ${p}/100 by dividing both numerator and denominator by ${g}; this keeps the value unchanged.`], `${p / 100} and ${p / g}/${100 / g}.`); }) },
      { h: "2. Find a percentage of an amount", body: ["Finding a percentage of an amount means dividing the whole into 100 equal percentage parts, then selecting the required number of those parts.", "We rarely need to draw all 100 parts. We can find 1%, 10%, 25% or another convenient benchmark and scale from it.", "Keep the whole and the percentage part labelled. A percentage below 100% must give a part smaller than the positive whole."], examples: percents.map((p, i) => { const t = totals[i], a = t * p / 100; return ex("percent_of_amount", `Find ${p}% of ${t} Ninefold apples.`, [`The whole is ${t} apples and it represents 100%.`, `Find one percentage part: 1% = ${t} ÷ 100 = ${t / 100} apples.`, `Select ${p} of those parts: ${t / 100} × ${p} = ${a} apples.`, `Because ${p}% is below 100%, ${a} should be below ${t}, which it is.`], `${a} apples.`); }) },
      { h: "3. Percentage increases and decreases", body: ["A percentage increase or decrease has two stages. First find the size of the change; then combine that change with the original amount.", "The percentage is calculated from the original amount, not from the final amount. Label the original as 100% before doing any arithmetic.", "An increase must finish above the original and a decrease must finish below it. That simple comparison catches many sign mistakes."], examples: percents.map((p, i) => { const t = totals[i], change = t * p / 100, down = i % 2 === 0, a = down ? t - change : t + change; return ex("percentage_change", `A quantity of ${t} is ${down ? "decreased" : "increased"} by ${p}%. What is the new value?`, [`The original ${t} represents 100%.`, `Find the change: ${p}% of ${t} = ${change}.`, `${down ? "A decrease removes the change, so subtract" : "An increase adds the change, so add"}: ${t} ${down ? "-" : "+"} ${change} = ${a}.`, `The result is ${down ? "below" : "above"} ${t}, which matches the word ${down ? "decreased" : "increased"}.`], `${a}.`); }) },
      { h: "4. Work backwards from a percentage", body: ["Sometimes the percentage part is known but the original whole is missing. We must reverse the forward calculation rather than subtracting the percentage number.", "If p% has a known value, divide that value by p to find 1%. Then multiply the one-percent value by 100 to rebuild the whole.", "A final forward check is essential: calculate the stated percentage of the recovered whole and make sure it returns to the given part."], examples: percents.map((p, i) => { const whole = totals[i], part = whole * p / 100; return ex("reverse_percentage", `${p}% of a number is ${part}. Find the whole number.`, [`The known ${part} represents ${p} equal percentage parts.`, `Find one part: 1% = ${part} ÷ ${p} = ${whole / 100}.`, `The whole is 100 such parts: ${whole / 100} × 100 = ${whole}.`, `Check forwards: ${p}% of ${whole} is ${part}.`], `${whole}.`); }) },
      { h: "5. Compare percentage information fairly", body: ["A percentage describes a share of its own whole. Two percentages cannot be compared as actual amounts until we know both wholes.", "Calculate each percentage amount separately and keep its label. Only then compare the two resulting quantities.", "This is why a smaller percentage of a large whole may exceed a larger percentage of a small whole."], examples: [[25, 120, 20, 200], [40, 150, 30, 240], [15, 300, 25, 160], [60, 90, 45, 140]].map(([p1, t1, p2, t2]) => { const a = p1 * t1 / 100, b = p2 * t2 / 100; return ex("compare_percent_amounts", `Which is greater: ${p1}% of ${t1}, or ${p2}% of ${t2}?`, [`Treat ${t1} and ${t2} as different wholes; do not compare ${p1}% and ${p2}% alone.`, `Calculate the first part: ${p1}% of ${t1} = ${a}.`, `Calculate the second part: ${p2}% of ${t2} = ${b}.`, `Compare the actual parts ${a} and ${b}.`], `${Math.max(a, b)} is greater.`); }) },
    ],
  };
}

function statisticsLesson() {
  return {
    title: "Statistics: reading the story in data", minutes: 22, order: 23,
    prereq: ["fourOperationsProblems", "fractionArithmetic", "decimalPlaceValue", "percentages"],
    intro: "Tables, charts and averages compress many observations into a form we can reason about. Read labels and units first, calculate only what the question asks and finish by interpreting the number in its data context.",
    sections: [
      { h: "1. Read a table accurately", body: ["A table organises information where a row and a column meet. Read the title and column headings before looking for a number.", "Trace along the named row and down the named column. Their intersection gives the value, but the heading supplies its meaning and unit.", "Say the category and unit in the answer. A bare number can be copied from the correct cell yet still fail to answer the question."], examples: [12, 18, 24, 31].map((v, i) => ex("read_data_table", `A harvest table shows Row ${i + 1}: ${v} baskets. How many baskets are recorded for Row ${i + 1}?`, [`The requested category is Row ${i + 1}, and the measured unit is baskets.`, `Locate Row ${i + 1} before reading across to its value.`, `The entry in that row is ${v}.`, "Attach the heading's unit to the number."], `${v} baskets.`)) },
      { h: "2. Find totals and differences", body: ["A total answers 'how many altogether?' and combines categories. A difference answers 'how many more or fewer?' and measures the gap.", "Read and label both data values before deciding which operation represents the question. Addition builds the total; subtraction finds the difference.", "Check a total is at least as large as each part. Check a difference by adding it to the smaller value to recover the larger."], examples: [[18, 27], [34, 19], [42, 36], [55, 28]].map(([a, b]) => ex("data_total_difference", `A table records ${a} apple baskets and ${b} pear baskets. Find the total and the difference.`, [`The two parts are ${a} apple baskets and ${b} pear baskets.`, `Combine them for the total: ${a} + ${b} = ${a + b}.`, `Subtract the smaller from the larger for the difference: ${Math.max(a, b)} - ${Math.min(a, b)} = ${Math.abs(a - b)}.`, `Check the difference: ${Math.min(a, b)} + ${Math.abs(a - b)} = ${Math.max(a, b)}.`], `Total ${a + b}; difference ${Math.abs(a - b)}.`)) },
      { h: "3. Calculate and interpret the mean", body: ["The mean is the value every observation would have if the total were shared equally. It is an equal-share model, not simply 'the middle'.", "First add every observation because the total amount must be preserved. Then divide by the number of observations because that is how many equal shares are required.", "The mean should lie between the smallest and largest values. If it does not, recheck the total and the divisor."], examples: [[6, 8, 10, 12], [14, 18, 22, 26], [9, 11, 15, 17], [20, 24, 28, 32]].map((values) => { const total = values.reduce((a, b) => a + b, 0), mean = total / values.length; return ex("mean_average", `Find the mean of ${values.join(", ")}.`, [`The observations are ${values.join(", ")}; all of them must contribute to the total.`, `Add them: ${values.join(" + ")} = ${total}.`, `There are ${values.length} observations, so make ${values.length} equal shares.`, `${total} ÷ ${values.length} = ${mean}. The result lies between ${Math.min(...values)} and ${Math.max(...values)}, as a mean should.`], `${mean}.`); }) },
      { h: "4. Read change on a line graph", body: ["A line graph shows change across an ordered variable, often time. Read the axis labels, scale and the two endpoint values before interpreting the segment.", "A segment rising from left to right shows an increase; a falling segment shows a decrease. The size of that change is the gap between the endpoint values.", "Describe both direction and amount with units. 'It went down' is incomplete when the graph lets us say how far."], examples: [[12, 19], [25, 17], [8, 23], [31, 20]].map(([a, b], i) => ex("line_graph_change", `A line graph shows ${a} baskets on day ${i + 1} and ${b} on the next day. Describe the change.`, [`Read the two endpoint values from the same vertical scale: ${a} and ${b} baskets.`, `The second value is ${b > a ? "higher" : "lower"}, so the graph shows a ${b > a ? "rise" : "fall"}.`, `Find the size of the change: ${Math.max(a, b)} - ${Math.min(a, b)} = ${Math.abs(a - b)}.`, `State direction, amount and unit together.`], `A ${b > a ? "rise" : "fall"} of ${Math.abs(a - b)} baskets.`)) },
      { h: "5. Connect pie charts, fractions and percentages", body: ["A full pie chart represents one whole, which is also 100%. A sector's angle and area show its share of that whole.", "Convert the sector fraction to a percentage by finding the equivalent amount out of 100. If the total number of items is known, apply the same fraction to that total.", "The percentage share and item count describe the same sector in different units. Check that both are sensible portions of their wholes."], examples: [[1, 4, 120], [2, 5, 150], [3, 10, 200], [3, 8, 240]].map(([n, d, total]) => { const pct = n / d * 100, amount = n / d * total; return ex("pie_chart_share", `${n}/${d} of a pie chart represents apple baskets. The chart covers ${total} baskets. What percentage and how many baskets is that?`, [`The sector is ${n} out of ${d} equal parts of the full chart.`, `Convert that share to an amount out of 100: ${n}/${d} × 100 = ${pct}%.`, `Apply the same fraction to the ${total}-basket whole: ${n}/${d} × ${total} = ${amount}.`, `Check that ${amount}/${total} simplifies to ${n}/${d}.`], `${pct}% and ${amount} baskets.`); }) },
    ],
  };
}

export function applyPrimaryNewTopics(lessons) {
  Object.values(lessons).forEach((lesson) => { if (lesson.order >= 16) lesson.order += 1; });
  lessons.percentages = percentageLesson();
  Object.values(lessons).forEach((lesson) => { if (lesson.order >= 23) lesson.order += 1; });
  lessons.statistics = statisticsLesson();
}
