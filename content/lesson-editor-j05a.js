import { applyEditorialRevisions, swc } from "./lesson-editor-runtime.js";
const headings=[
["1. Gradient measures a constant rate",["Plot two points, then imagine the smallest right-angled step from one to the other. Gradient is vertical change divided by horizontal change: change in y per change in x.","A positive gradient rises left to right, a negative gradient falls and a zero gradient stays level. The order of both differences must match."]],
["2. An equation makes coordinate pairs",["In y=mx+c, choose an x-value, multiply it by m and then add c. The resulting pair (x,y) is one point on the line.","A table of several substitutions is a reliable way to plot a graph and to check that a coordinate satisfies an equation."]],
["3. Intercept and gradient tell the whole story",["The intercept c is the y-value when x=0: where the line crosses the vertical axis. The gradient m tells how y changes for every one step in x.","Read the same facts from a table, equation or graph. They are three descriptions of one constant-rate relationship."]],
["4. Linear models start with a value and change steadily",["A linear model fits a situation with a fixed starting amount and a constant change per unit: y=mx+c.","Attach meaning and units to m and c. A starting fee is an intercept; a cost per mile or pages per minute is a gradient."]],
["5. A crossing is an equal-output moment",["Where two lines cross, they have the same x-value and the same y-value. In a story this can mean two plans cost the same or two journeys reach the same position.","Use substitution to check both equations at a proposed intersection and interpret the coordinate with its units."]]
];
const states=[["Two coordinates are known; the required rate compares their vertical and horizontal changes.","The line's rise and run can be read from the given points without using its picture alone.","A negative-coordinate pair still has a horizontal and vertical difference whose order must agree.","We need decide whether the calculated change is per one x-unit and state its direction."],["The equation and a chosen x-value are known; calculate the matching y-coordinate.","A coordinate pair is proposed, so test it by replacing x and comparing the calculated y.","Several x-values are needed to make a table that reveals the same straight-line relationship.","The missing coordinate must make both sides of the equation equal."],["The line's equation contains m and c, and we need identify what each does to a plotted point.","The y-axis crossing is found by using x=0, not by guessing from another point.","The gradient is a ratio of changes, so use a vertical change and its matching horizontal change.","A table, graph and equation must agree on the same intercept and one-step change."],["The context gives an initial amount and a repeated change, which correspond to c and m.","The requested model needs units attached to both the starting value and the rate.","A predicted value comes from applying the constant rate to the stated input then including the start.","The model is only sensible if its intercept and gradient match the story's initial condition and direction."],["Two rules describe outputs for the same input; the crossing requires a shared pair.","A proposed x-value must give equal y-values in both equations.","The intersection coordinates need an interpretation in the situation, not just algebraic symbols.","The two changing quantities must be compared at the same x-value before declaring them equal."]];
const checks=[["Use the gradient to move from one given point by the stated horizontal change and recover the other.","Reverse the run and rise together; the quotient should stay the same.","Substitute both points into the resulting line relationship to verify the direction and size of change.","A one-unit increase in x should change y by exactly the calculated gradient."],["Put the calculated pair back into y=mx+c and verify both sides match.","Substitution must reproduce the coordinate's stated y-value.","Check two rows of the table in the equation before drawing the line through them.","The completed coordinate makes the equation true when both values are substituted."],["Set x=0 and check that the result is the stated intercept.","Move one x-unit from the intercept and verify that y changes by m.","Compare the rise/run calculation with the equation's coefficient of x.","A coordinate table generated from the equation should show the same constant difference."],["At input zero, the model should return the starting amount from the story.","Increase the input by one unit and check that the output changes by the stated rate with its unit.","Substitute the story input and see whether the answer has sensible size and units.","Compare a second point from the context with the model to test the constant-rate assumption."],["Substitute the intersection x into both equations and confirm the same y.","The two calculated outputs must match, not merely be close on a sketch.","Use the intersection's x and y with the story units to check the stated conclusion.","A value just before and after the crossing can confirm which line is higher on each side."]];
function examples(old,s){return old.map((e,i)=>swc({q:e.q,state:states[s][i],steps:e.steps,answer:e.answer,conclusion:e.answer,check:checks[s][i],structureId:e.structureId,difficulty:e.difficulty}));}
export function applyJuniorEditorJ05A(lessons){const old=lessons.linearGraphs.sections;applyEditorialRevisions(lessons,{linearGraphs:{intro:"A straight-line graph is a picture of a constant relationship. We will build it from coordinate pairs and tables, read its gradient as a rate and its intercept as a starting value, then use crossings to compare two situations fairly.",sections:headings.map(([h,body],s)=>({h,body,examples:examples(old[s].examples,s)}))}});}
