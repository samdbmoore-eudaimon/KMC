import { applyEditorialRevisions, swc } from "./lesson-editor-runtime.js";
const h=[["1. A logic grid matches each attribute once",["A logic grid records a one-to-one matching: each person, object or position has one value from each attribute, and each value is used once unless stated otherwise.","Mark direct matches first, then cross out every incompatible partner in the same row and column."]],["2. Exclusions shrink candidate lists",["A ‘not’ clue removes a candidate; it does not immediately identify the correct match. Keep a visible candidate list until only one possibility remains.","When one row has one candidate left, that match is forced and its value can be crossed out everywhere else."]],["3. Linked clues make deduction chains",["One match can connect several attributes. Follow the link through the grid: if A has blue and blue is Tuesday, then A is Tuesday.","Record each consequence separately so that a chain is traceable and can be checked against the original clues."]],["4. Order of checking does not change the solution",["Any correct clue can be applied first. A systematic grid prevents repeated work because every mark represents a precise logical fact.","If a temporary assumption leaves a row with no possible value or gives one value twice, it is a contradiction and the assumption is rejected."]],["5. More attributes require complete verification",["A third attribute such as a box, day or score creates a second linked grid. Check every person has one value in every category and every value is used once.","A finished-looking grid is unique only if every alternative candidate has been eliminated or leads to contradiction."]]];
const st=[["The people and attribute values form a one-to-one matching, so each row and column needs one final match.","A direct clue gives a definite pair that can be marked before using exclusions.","A value used by one person cannot be used by another, so its column consequences follow immediately.","The answer needs a complete matching, not only one identified pair."],["The clue forbids a specific pairing, so remove only that candidate from the grid.","The row or column has remaining candidates that must be counted before declaring a forced match.","A value eliminated from all but one row is forced there by the one-use rule.","The final match must be justified by all exclusions, not guessed from one negative clue."],["A known match connects two attributes and can transfer information to a third.","The deduction chain needs each intermediate attribute written down in the correct direction.","A new forced match removes its value from every competing row or column.","The conclusion must satisfy the original linked clues as well as the derived chain."],["Several clues can be entered in any order; choose one and make only consequences that follow from it.","A temporary candidate is being tested, so record it separately from confirmed marks.","The test must check for a duplicate value or an empty candidate list, which is a contradiction.","The surviving case needs a full check; it is not enough that the other case failed."],["Each person needs one value in each of the linked attributes, with one use of every value.","A candidate matching must be checked across all grids, not just the first attribute pair.","Uniqueness requires considering whether any unmarked alternative can also satisfy every clue.","The final answer should list the complete matched set with its attributes."],["Count one confirmed match in each required row and column.","Verify every crossed-out candidate conflicts with a direct clue or one-use consequence.","Trace the linked attributes back through the grid and recover each original clue.","For a contradiction, identify the exact duplicate or empty row that invalidates the assumption."]];
const c=[["Every person and every value should appear in exactly one final match.","Use the direct match to confirm all rival cells in its row and column are excluded.","Check no value is assigned twice after completing the grid.","Read the completed matching against every original clue."],["Count the surviving candidates in the row or column and verify a forced match has exactly one.","The excluded pairing must be absent while other permitted pairings remain until ruled out.","Check the one remaining row for a value before marking it as forced.","Substitute the final pairing into all negative and positive clues."],["Write the two links in sequence and verify the endpoint follows from both.","Check the intermediate value is used consistently in each connected grid.","Confirm the forced match removes that value from all competitors.","Read the finished chain backwards to reproduce the original facts."],["Applying the same clues in a different order should leave the same confirmed and excluded cells.","A contradiction must show an impossible duplicate or no remaining candidate explicitly.","Reject only the temporary assumption, then retest the remaining case from original clues.","Verify the surviving case assigns every value once and satisfies all statements."],["Check each person has exactly one entry in every attribute category.","Check every category value is used once across the final grid.","Try or rule out each remaining alternative to justify uniqueness.","The completed table must satisfy direct clues, exclusions and linked deductions together."]];
function ex(old,s){return old.map((e,i)=>swc({q:e.q,state:st[s][i],steps:e.steps,answer:e.answer,conclusion:e.answer,check:c[s][i],structureId:e.structureId,difficulty:e.difficulty}));}
export function applyJuniorEditorJ11C(lessons){const old=lessons.gridLogic.sections;applyEditorialRevisions(lessons,{gridLogic:{intro:"Logic grids turn clues into visible candidates and exclusions. We will enter direct matches, trace linked attributes, use contradictions only when needed and check every completed grid for one-to-one matching and genuine uniqueness.",sections:h.map(([x,body],s)=>({h:x,body,examples:ex(old[s].examples,s)}))}});}
