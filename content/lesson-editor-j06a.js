import { applyEditorialRevisions, swc } from "./lesson-editor-runtime.js";
const headings=[["1. Organise data before summarising",["A list becomes easier to read when it is ordered or counted in a frequency table. Keep every observation: a summary built from missing data is not a summary of the real set.","Ordering reveals the smallest, largest and middle positions. Frequency reveals which value occurs most often."]],["2. Mean, median and mode answer different questions",["Mean is the equal-share value: add all observations and divide by how many there are. Median is the middle position after ordering. Mode is the most frequent value.","Choose a measure for its job. An outlier can pull the mean strongly, while the median often stays closer to the typical middle observation."]],["3. Range measures spread",["Range is largest minus smallest. It describes how spread out a set is, not its typical value.","Changing one extreme can change the range dramatically; changing a middle value may leave it unchanged. Compare both centre and spread when judging consistency."]],["4. Read graph claims against the data",["Read axes, labels and scales before accepting a graph's visual message. A truncated vertical axis can make a small numerical gap look huge.","Use the actual values to calculate a summary or compare groups. A conclusion should say exactly what the data supports, no more."]]];
const states=[["The raw observations are given; we need arrange or count them without losing repeats.","The data set contains values whose positions must be visible before selecting a middle or extreme.","A frequency table groups equal observations, so the total frequency must still equal the number originally recorded.","The requested summary depends on an ordered list, not the order in which values happened to be written."],["Every observation contributes to the total and count needed for an equal-share mean.","The median is determined by central position after ordering, while the mode depends on repeated frequency.","The data contains an extreme value, so compare what it does to the mean and median.","The question asks for a suitable typical value, so identify which measure is least distorted by the stated data."],["The smallest and largest recorded values determine the spread requested as range.","Two sets may have similar centres but need their extreme values compared for consistency.","A changed observation must be checked to see whether it becomes a new minimum or maximum.","The conclusion needs both a centre measure and range, because either one alone leaves out information."],["The graph provides values and a scale; the claim must be tested with numerical differences.","A bar's visible height can mislead if its axis does not start at zero.","The stated graph values can be used to calculate a valid summary before interpreting a trend.","A conclusion about change must inspect all relevant points, not only the first and last."]];
const checks=[["Count the ordered entries and compare with the original number of observations.","Read the central position and extremes back from the sorted list to confirm they are unchanged values.","Add all frequencies and verify they equal the raw-data count.","Reorder the same values a second way and confirm the requested positional summary is unchanged."],["Multiply the mean by the count to recover the total of the data.","Count equal numbers of ordered observations on each side of the median, or average the two central ones.","Remove or alter the extreme and compare the resulting mean and median to confirm its effect.","Check the chosen measure answers the contextual idea of typical, frequent or equal-share value."],["Add the calculated range to the minimum to recover the maximum.","Compare both minima and maxima to verify which set has the larger numerical spread.","Recalculate the two extremes after the stated change before deciding whether the range changes.","Use the original values to verify the reported centre and range together."],["Subtract the read values to verify the numerical size of the graph's claimed difference.","Compare the axis baseline with the labels and calculate the actual ratio or gap.","Check the calculated statistic lies between the data's smallest and largest values where appropriate.","Test the claim against each consecutive data point so an overall trend is not mistaken for every-step change."]];
function examples(old,s){return old.map((e,i)=>swc({q:e.q,state:states[s][i],steps:e.steps,answer:e.answer,conclusion:e.answer,check:checks[s][i],structureId:e.structureId,difficulty:e.difficulty}));}
export function applyJuniorEditorJ06A(lessons){const old=lessons.statisticsMeasures.sections;applyEditorialRevisions(lessons,{statisticsMeasures:{intro:"Data summaries answer different questions. We will organise a real data set, share its total equally for the mean, locate its middle for the median, count its most frequent value for the mode and measure its spread with range, always checking the conclusion against the observations.",sections:headings.map(([h,body],s)=>({h,body,examples:examples(old[s].examples,s)}))}});}
