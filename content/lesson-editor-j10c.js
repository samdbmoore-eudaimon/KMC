import { applyEditorialRevisions, swc } from "./lesson-editor-runtime.js";
const heads=[["1. A guarantee survives the worst case",["‘Possible’ means one arrangement can work. ‘Guaranteed’ means every arrangement must work. To prove a guarantee, imagine an adversary spreading objects as evenly as possible to delay it.","Name the objects and the boxes or categories before counting."]],["2. Sock guarantees come from delaying a match",["For one pair from colour categories, the worst case takes one sock of each colour first. The next sock forces a repeated colour.","The order of drawing does not matter to the guarantee because the argument covers every possible order."]],["3. The pigeonhole principle counts objects into boxes",["If more objects are placed in fewer boxes, some box must contain at least two objects. This is a certainty claim, not a probability estimate.","The useful question is what the objects are and what counts as a box in the particular story."]],["4. Larger guarantees use even spreading",["To guarantee at least k objects in one box, first place at most k-1 in every box. One further object forces a box to reach k.","This worst-case arrangement proves the minimum number needed and shows why one fewer does not guarantee it."]],["5. Translate the cards or objects into categories",["Card and selection puzzles often hide the boxes in properties such as suit, rank, colour or month. Choose the category that matches the requested repeated feature.","Check whether objects can belong to more than one category before applying a simple pigeonhole count."]]];
const st=[["The wording asks for possibility or certainty; decide which kind of conclusion is required.","Objects are placed into categories, so identify the objects and boxes explicitly.","A proposed guarantee needs a worst-case arrangement that tries to avoid it.","The answer must distinguish what could happen from what must happen."],["The sock colours are the boxes and drawn socks are the objects; delay a matching pair as long as possible.","One sock from each colour gives the largest match-free draw, so count that before the forcing draw.","The requested repeated feature may be a pair or a larger same-colour group.","The guarantee must hold for any draw order, including the least helpful order."],["The numbers of objects and boxes are known; compare them to see whether some box must repeat.","The story terms need translating into a precise object-box model before using the principle.","A conclusion about at least two requires more objects than boxes, not merely equal numbers.","The proof needs exhibit the most even possible allocation and show why the next object breaks it."],["The desired minimum in one box is k, so construct the fullest arrangement that still avoids k.","Place k-1 objects in every box before adding the one that forces the guarantee.","The number one below the guarantee threshold must be shown possible without reaching k.","The conclusion is a minimum forced occupancy, not an exact description of every box."],["The card or object property requested determines the correct boxes for the count.","The categories must be disjoint or their overlap must be handled before a pigeonhole argument.","The worst-case selection fills all non-forcing category places first.","The final claim must be checked against the requested property rather than a different repetition."],["Write the delaying allocation and count its objects to show the guarantee has not yet occurred.","Add one more object and identify the box that must now exceed the safe limit.","Construct an example with one fewer object to prove the stated number is minimal.","Check every possible object is assigned to one of the named boxes under the chosen classification."]];
const checks=[["Display a counterexample for a merely possible claim or a worst-case proof for a forced claim.","Count objects in each named category and confirm every object has one assigned box.","Verify the delaying arrangement avoids the requested repetition.","After the forcing object, use the pigeonhole count to show no alternative arrangement avoids the result."],["Count one sock in each colour and confirm no pair has yet appeared.","The next sock must share one of the listed colour boxes, forcing a pair.","For a larger group, verify the worst case has at most the stated safe number of each colour.","The argument should work without knowing the order in which socks were drawn."],["Compare the object count with the box count and identify the forced repeated box.","Show that an allocation of at most one per box holds only up to the number of boxes.","Check that equality of objects and boxes can avoid a repeat, so ‘more than’ is necessary.","Map the original story back to the object-box labels used in the proof."],["Multiply boxes by k-1 to verify the largest no-k arrangement.","Add the next object and show some box must contain k.","Give or describe the one-fewer allocation that avoids k, proving minimality.","The result says at least k in one box, not exactly k or in every box."],["List the categories and check they match the repeated card feature asked for.","Verify whether any card could be counted in two boxes and adjust the reasoning if so.","Count the worst-case draw that avoids the requested matching property.","The next draw must force the requested property, not merely another shared feature."]];
function ex(old,s){return old.map((e,i)=>swc({q:e.q,state:st[s][i],steps:e.steps,answer:e.answer,conclusion:e.answer,check:checks[s][i],structureId:e.structureId,difficulty:e.difficulty}));}
export function applyJuniorEditorJ10C(lessons){const old=lessons.pigeonhole.sections;applyEditorialRevisions(lessons,{pigeonhole:{intro:"The pigeonhole principle proves what must happen by imagining the least helpful possible arrangement. We will identify objects and boxes, spread objects evenly to delay a result and add the first object that makes the conclusion unavoidable.",sections:heads.map(([h,body],s)=>({h,body,examples:ex(old[s].examples,s)}))}});}
