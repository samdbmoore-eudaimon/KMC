import { applyEditorialRevisions, swc } from "./lesson-editor-runtime.js";
const heads=[["1. Constant first differences make a line",["Write the change between neighbouring y-values when x rises by equal steps. A constant first difference signals a linear relationship and a straight graph.","The constant difference is the rate of y change per one x-step, not the starting value."]],["2. Quadratic patterns have changing first differences",["For a quadratic table with equally spaced x-values, first differences change but second differences are constant. Square-number growth often adds successive odd amounts.","This creates a curved parabola, not a line, because the rate itself changes steadily."]],["3. Multiplying and reciprocals create other curves",["Exponential growth multiplies by a constant factor for equal x-steps, so its differences grow. Reciprocal relationships keep a product constant, so one value decreases as the other increases.","Use division to test a repeated multiplier and multiplication to test a constant product."]],["4. Graph shape tells a story of changing rate",["A curved graph means the rate is not constant. Read coordinates, direction and steepness in context before naming a relationship.","Tables, repeated operations and graphs should agree. Test a proposed rule on several known inputs before using it to predict."]]];
const st=[["The table has equal x-steps; calculate consecutive y changes to test whether the rate is constant.","The first value and repeated difference determine a linear position rule.","A graph or context may look increasing, but only equal first differences prove linearity.","The requested next value must continue the same additive change from the last known term."],["The y-values change by unequal first differences, so compare the changes of those changes.","A growing arrangement can be counted in successive layers to reveal the changing additions.","The table uses equal x-steps, allowing second differences to test for quadratic structure.","A proposed quadratic rule must reproduce several known values, not merely one."],["The values may be linked by multiplication, so compare consecutive ratios rather than differences.","An inverse context gives two quantities whose product may remain fixed.","Repeated multiplication predicts the next value from a current value and a common factor.","The relationship type must be identified from the operation that stays constant."],["The graph's coordinates and curve direction are known; interpret the rate change rather than assuming a straight line.","A context supplies units, so the graph's steepness describes a changing rate with those units.","A table and graph must be checked against the same input-output pairs.","A prediction beyond the shown values needs a rule supported by repeated operations or data."]];
const checks=[["Subtract each neighbouring pair and verify all first differences match.","Use the rule to reproduce the first and a later table value.","A straight-line claim should give the same y-change for every equal x-change.","Add the common difference to the last listed term and recover the predicted answer."],["Subtract successive first differences and verify the second differences match.","Count the new pieces in adjacent growing figures and compare with the predicted difference pattern.","Use the proposed quadratic rule at two or more x-values and check the table outputs.","A constant second difference with changing first differences supports a quadratic, not linear, pattern."],["Divide each term by its predecessor and verify the multiplier is constant.","Multiply the paired inverse quantities and check the same product each time.","Apply the common factor once more and verify the next predicted value.","Check the calculated pattern has changing differences, so it is not mistaken for a linear one."],["Read two or more points and compare their rate changes with the curve's steepness.","Use the stated units to check the graph interpretation answers the actual context.","Substitute displayed x-values into the rule and reproduce plotted y-values.","Treat extrapolation cautiously and check it follows the established operation rather than just a visual guess."]];
function ex(old,s){return old.map((e,i)=>swc({q:e.q,state:st[s][i],steps:e.steps,answer:e.answer,conclusion:e.answer,check:checks[s][i],structureId:e.structureId,difficulty:e.difficulty}));}
export function applyJuniorEditorJ08B(lessons){const old=lessons.nonLinearRelations.sections;applyEditorialRevisions(lessons,{nonLinearRelations:{intro:"Tables and graphs reveal relationships through the operation that repeats. We will separate constant addition from changing quadratic growth, constant multiplication and constant-product reciprocal patterns, then use several data points to test a proposed rule.",sections:heads.map(([h,body],s)=>({h,body,examples:ex(old[s].examples,s)}))}});}
